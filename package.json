{
  "name": "reply-guyz",
  "private": true,
  "scripts": {
    "build": "cd dashboard-app && npm install && npm run build && mkdir -p ../dashboard && cp -r dist/* ../dashboard/",
    "dev": "cd dashboard-app && npm run dev"
  }
}
