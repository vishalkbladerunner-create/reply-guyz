{
  "name": "replyguyz-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm install && npm run build && cp -r dist ../dashboard",
    "dev": "npx serve ."
  }
}
