// ReplyGuyz Dashboard Runtime Configuration
// Edit these values with your actual Supabase credentials, then redeploy.
// This file is loaded BEFORE the React app so env vars are available at runtime.

window.ENV = {
  // Get these from Supabase Dashboard → Project Settings → API
  VITE_SUPABASE_URL: '',
  VITE_SUPABASE_ANON_KEY: '',
}
